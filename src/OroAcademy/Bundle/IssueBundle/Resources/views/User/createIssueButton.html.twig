{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientButton({
    'dataUrl': path('oroacademy_create_issue', oro_action_params(entity, 'assign')),
    'aCss': 'no-hash',
    'iCss': 'icon-file-alt',
    'dataId': entity.id,
    'label' : 'oroacademy.buttons.create_issue.label'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'issue-dialog',
            'dialogOptions' : {
                'title' : 'oroacademy.buttons.create_issue.label'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 500,
                autoResize: true,
                modal: true,
                minHeight: 100
            }
        }
    }
}) }}
