{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroEntityConfigBundle::macros.html.twig' as entityConfig %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderHtmlProperty('Description'|trans, entity.description) }}
            {{ UI.renderProperty('Reported by'|trans, entity.reporter ? entity.reporter.fullName : '') }}
            {{ UI.renderProperty('Assigned to'|trans, entity.assignee ? entity.assignee.fullName : '') }}
            {{ UI.renderHtmlProperty('Collaborators'|trans, entity.getSimpleCollaboratorArray()|join('<br />')) }}
            {{ UI.renderHtmlProperty('Related issues'|trans, entity.getPrettyRelatedIssues()|join('<br />')) }}
            {{ UI.renderProperty('Created at'|trans, entity.createdAt|date) }}
            {{ UI.renderProperty('Updated at'|trans, entity.updatedAt|date) }}
        </div>
    </div>
</div>
